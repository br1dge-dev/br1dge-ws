export default function Hero() {
  return (
    <section className="relative h-screen w-full overflow-hidden bg-black flex items-center justify-center">
      <span className="text-[30rem] font-bold leading-none">
        ͆
      </span>
    </section>
  );
}
